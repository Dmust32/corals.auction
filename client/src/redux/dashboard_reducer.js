import axios from 'axios'

const initialState = {}

// ACTION TYPES

function reducer(state= initialState, action){
    switch(action.type){
        default:
            return state
    }
}

export default reducer